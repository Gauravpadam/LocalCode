version: '3.9'

services:
  postgres:
    image: postgres:15-alpine
    ports:
      - 5175:5432
    volumes:
      - ~/cheetcodedata/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_DB=${DB_NAME}